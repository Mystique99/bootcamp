// Pregunta 1

// esta funcion ejecuta la multiplicacion  por 4 de los elementos dentro de un arreglo y los retorna
multiplyByFour = (arr) => arr.map((elem) => elem * 4);

// esta funcion verifica si un numero del arreglo es mayor que ocho y los retorna
TakeGratherThanEight = (arr) => arr.filter((elem) => elem > 8);